# Database (read-only) — set these in Amplify Hosting environment variables
DB_HOST=your-postgres-host.rds.amazonaws.com
DB_PORT=5432
DB_NAME=your_db
DB_USER_RO=readonly_user
DB_PASS=readonly_password
# Set to 'false' only for local/dev without SSL (not recommended in prod)
DB_SSL=true
